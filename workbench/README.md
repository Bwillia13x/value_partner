# UX Workbench (Streamlit)

Interactive dashboard for exploring factor datasets and backtest results.

Run locally:
```bash
pip install -r workbench/requirements.txt
streamlit run workbench/streamlit_app.py
```